const styles = theme => ({
    root: {
      display: 'flex',
      'justify-content': 'center',
      'flex-direction': 'column',
      'align-items': 'center'
    },
    formControl: {
      margin: theme.spacing.unit * 3,
    },
    checkBoxesList: {
      'padding-left': '20px'
    }
  });

export default styles;  